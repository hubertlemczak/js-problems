# Pseudo Code Solution

```
OBJECT coins
FUNC num
  FOR i UNTIL coins.length
    let remainder;
    UPDATE OBJECT round down num / coin
    let coinValue = coin * amount
    IF coinValue > 0
      remainder = num % coinValue
```
